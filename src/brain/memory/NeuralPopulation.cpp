// NeuralPopulation.cpp — Implementation stubs (most logic is inline in the header).
// This file exists for the CMake source registration and any future out-of-line implementations.
#include "NeuralPopulation.h"

// All PopulationNode and NeuralWorkspace methods are defined in the header as inline.
// This translation unit is intentionally minimal.
namespace yuki::memory {
// (no additional definitions needed — all methods are inline in NeuralPopulation.h)
} // namespace yuki::memory
